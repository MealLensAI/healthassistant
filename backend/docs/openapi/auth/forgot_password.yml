tags:
  - Auth
summary: Request password reset email
consumes:
  - application/json
security: []
parameters:
  - in: body
    name: body
    description: Request body
    required: true
    schema:
      type: object
      required:
        - email
      properties:
        email:
          type: string
        redirect_url:
          type: string
responses:
  200:
    description: Success

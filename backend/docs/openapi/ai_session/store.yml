tags:
  - AI Session
summary: Store AI session (path is /api/api/store-session as registered)
consumes:
  - application/json
security:
  - Bearer: []
parameters:
  - in: body
    name: body
    description: Request body
    required: true
    schema:
      type: object
      required:
        - session_data
      properties:
        session_data:
          type: object
          description: Contains prompt, response, timestamp, optional metadata
responses:
  201:
    description: Created
  400:
    description: Bad request
  401:
    description: Unauthorized
  500:
    description: Server error
